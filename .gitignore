# This .gitignore file is used to specify files and directories that should be ignored by Git version control.
.env

# Removing the sqlite database file from version control to avoid conflicts and ensure that each user can have their own local database.
weather_data.db
